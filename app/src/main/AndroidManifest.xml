<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.wit.adventurio">

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ForgotPasswordActivity"></activity>
        <activity android:name=".activities.TripsListAccount" />
        <activity android:name=".activities.DeleteAccountActivity" />
        <activity android:name=".activities.WalkingTripCreateActivity" />
        <activity android:name=".activities.WalkingTripTrackingActivity" />
        <activity android:name=".activities.TripSelectActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".activities.WalkingStatsActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".activities.LoginActivity"> <!-- change to login -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>